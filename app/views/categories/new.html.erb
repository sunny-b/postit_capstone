<h3>New Category</h3>

<%= render 'shared/errors', item: @category %>

<div class="span8 well">
  <%= form_for @category do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <br />
    <%= f.submit %>
  <% end %>
</div>
